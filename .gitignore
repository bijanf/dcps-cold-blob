# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
*.egg-info/
*.egg
build/
dist/
.venv/
venv/

# LaTeX build artifacts
manuscript/build/
*.aux
*.log
*.bbl
*.blg
*.out
*.toc
*.lof
*.lot
*.fls
*.fdb_latexmk
*.synctex.gz

# Analysis caches (regenerable from scripts)
dcps/cache/

# Stale/old figures not used in current manuscript
dcps/figures/

# Bulk external data (downloadable from upstream sources)
data/external/hadisst/
data/external/palmod_130k/lipd_unzipped/
data/external/palmod_130k/PalMod2_0_0.zip
data/external/atmospheric/

# Editor / OS artifacts
.DS_Store
.vscode/
.idea/
*.swp
*~

# Secrets and credentials (just in case)
*.env
*.pem
credentials.json
token.json
secrets/
.aws/
# Local CEDA credential install script - NEVER commit
add_ceda_to_netrc.sh
*.netrc
.netrc

# Background-process logs (may contain Pangeo URLs or auth artefacts)
logs/

# External-drive symlink and large external dataset mirrors
data/external_drive/
data/external/

# P6 PIK bundle staging area (canonical files copied into data/external/p6/)
p6_results/
p6_bundle.tar.gz

# Sharable manuscript bundles built by `zip -r ... manuscript/...`
manuscript-bundle-*.zip
manuscript-bundle-README.md

# Tool/assistant artefacts - never commit any conversation, session,
# planning, or tooling output. Belt and suspenders so future automation
# cannot accidentally leak working notes into the public repo.
.claude/
.claude*
CLAUDE.md
.gemini/
GEMINI.md
.cursor/
.anthropic/
.openai/
*.claude.md
*.gemini.md
*conversation*.md
*session*.md
*transcript*.md
*chat*.md
*handoff*.md
# Bundle dumps produced by repo-context tools (repomix etc.)
repomix-output.*

# Local workspace symlink to the private manuscript repo
# (`ln -s ../coldblob-manuscript/manuscript manuscript`). Figure-writing
# scripts resolve `manuscript/figs/...` through this symlink when both
# repos are checked out side by side. Never tracked in the public repo.
/manuscript
/manuscript/

# Companion-paper LaTeX sources (live in the private manuscript repo)
quiescence_toolkit/paper/
